# Regra operacional

O cargo nominal do colaborador não concede acesso automaticamente. A concessão ocorre pelo perfil de acesso associado ao usuário, permitindo que duas pessoas com cargos semelhantes tenham escopos diferentes quando a operação exigir.
