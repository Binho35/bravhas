# Revisão

A solução evita inflar `UserRole` com cargos organizacionais e permite que a empresa altere permissões sem deploy. A persistência separada também mantém compatibilidade com autenticação/financeiro. O principal trabalho subsequente é o rollout do enforcement granular nos fluxos existentes.
